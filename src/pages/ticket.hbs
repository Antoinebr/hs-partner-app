<!DOCTYPE html>
<html lang="en">

<head>

    <meta charset="utf-8" />
    <link rel="icon" href="https://glitch.com/favicon.ico" />
    <script src="https://cdn.tailwindcss.com"></script>
    <meta name="viewport" content="width=device-width, initial-scale=1" />

    <title>{{seo.title}}</title>

    <!-- Meta tags for SEO and social sharing -->
    <link rel="canonical" href="{{seo.url}}">
    <meta name="description" content="{{seo.description}}">
    <meta property="og:title" content="{{seo.title}}">
    <meta property="og:type" content="article">
    <meta property="og:url" content="{{seo.url}}">
    <meta property="og:description" content="{{seo.description}}">
    <meta property="og:image" content="{{seo.image}}">
    <meta name="twitter:card" content="summary">

</head>

<body>


    <!-- Menu -->
    {{> menu }}
    <!-- Menu -->

    <div class="wrapper">
        <div class="content" role="main">

            <!-- This is the start of content for our page -->
            <div class="container mx-auto">

                <div class="bg-white h-5/5 px-9 py-9">

                    <section class="grid grid-cols-1 gap-2 mt-10">
                        
                        {{#if tickets}}
                        
                                <article class="filter border drop-shadow-xl rounded-2xl overflow-hidden relative">
                                    <div class="content px-4">
                                         <div class="text-xs mt-3 mb-2 border rounded-2xl w-max p-2 bg-green-500 text-white">{{tickets.properties.hs_ticket_category}}</div>
                                        <p class="text-l font-bold mt-3 mb-2">{{tickets.properties.subject}} </p>
                                        <p class="text-xs mt-3 mb-2"> {{{tickets.properties.content}}}</p>
                                       
                                        <p class="text-xs mt-3 mb-2">{{formatDate tickets.properties.createdate}}</p>
                                    </div>
                                </article>                     
                        {{/if}}

                    </section>

                </div>

            </div>

        </div>
    </div>

</body>
  <!-- footer -->
    {{> footer }}
    <!-- footer -->
</html>